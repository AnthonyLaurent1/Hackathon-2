{% extends 'base.html.twig' %}

{% block title %}Mon projet {% endblock %}

{% block body %}

    <div class="container text-center">
        <div class="d-flex flex-column">
            <div class="d-flex flex-wrap">
                {% for product in products %}
                    <div class="col-md-4 mt-5 block1">
                        <div class="row">
                            <div class="col-md-12">
                                    <div class="card"> <img class="card-img-top card-show" src="{{ product.picture }}" alt="Card image cap">
                                        <div class="card-body">
                                            <h4 class="card-title">{{ product.name }}</h4>
                                            <p class="card-text">{{ product.description}}</p>
                                            <p class="card-text">Prix : {{ product.price }} €</p>
                                            <p class="card-text">Notes : {{ product.ranking }}/5</p>
                                            <a href="#" class="btn shadow-none">Ajouter au panier</a>
                                        </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <a class="btn col-4 mx-auto shadow-none rounded-pill p-4 fs-5 mt-5 mb-5" href="{{ path('home_project') }}">Recommencer</a>
    </div>
{% endblock %}
